import "./styles.css";

const Button = () => {
  return (
    <div className="btn-container">
      <button className="btn main-btn">
          <h5>VER CATÁLOGO</h5>
      </button>
    </div>
  );
};

export default Button;
